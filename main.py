# Making a library for processing and correlating data between a reference set and a regular set of data

import pandas as pd
import numpy as np
import random
import datetime as dt



def match_df_formats(data, ref_data):
    # this part I will set up related to wind projects, will change later
#     TODO: check if the inputs are actually Dataframes
#     Get the relevant data columns
#     Sort the dataframes by the appropriate month
#     Compare them on a monthly basis with a loop
#     get monthly correlation factors
#     plot a scatterplot with a trendline based on the ref_data
#     save scatterplot as png
#     plot monthly data averages for data and ref data in a line plot
#     save monthly data line graph as png
#     Save the monthly correlation factors for each month into a new dataframe

